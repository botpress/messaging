{
  "name": "@botpress/messaging-socket",
  "version": "0.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "source": "src/index.ts",
  "license": "AGPL-3.0",
  "scripts": {
    "build": "yarn --silent && tsc --build",
    "watch": "yarn --silent && tsc --build --watch",
    "prepublishOnly": "yarn --silent --frozen-lockfile && tsc --build"
  },
  "files": [
    "dist"
  ],
  "devDependencies": {},
  "dependencies": {
    "@botpress/messaging-base": "*",
    "socket.io-client": "^4.4.0"
  }
}
