{
  "name": "@botpress/messaging-board",
  "version": "0.0.1",
  "source": "src/index.html",
  "license": "AGPL-3.0",
  "scripts": {
    "build": "yarn --silent && yarn run -T parcel build",
    "watch": "yarn --silent && yarn run -T parcel watch",
    "dev": "yarn --silent && yarn run -T parcel"
  },
  "devDependencies": {
    "@types/react": "^17.0.38",
    "@types/react-dom": "^17.0.11"
  },
  "dependencies": {
    "@botpress/messaging-client": "*",
    "@botpress/messaging-socket": "0.0.2",
    "@botpress/webchat": "0.0.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
