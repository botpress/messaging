{
  "name": "@botpress/messaging-board",
  "version": "0.1.0",
  "source": "src/index.html",
  "license": "AGPL-3.0",
  "scripts": {
    "build": "yarn && yarn run -T parcel build",
    "watch": "yarn && yarn run -T parcel watch",
    "dev": "yarn && yarn run -T parcel"
  },
  "devDependencies": {
    "@parcel/config-default": "^2.2.1",
    "@parcel/transformer-typescript-tsc": "^2.2.1",
    "@types/react": "^17.0.38",
    "@types/react-dom": "^17.0.11",
    "typescript": ">=3.0.0"
  },
  "dependencies": {
    "@botpress/messaging-base": "*",
    "@botpress/messaging-client": "*",
    "@botpress/messaging-socket": "*",
    "@botpress/webchat": "*",
    "react": "^17.0.2",
    "react-dom": "^17.0.2"
  }
}
