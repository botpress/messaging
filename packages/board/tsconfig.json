{
  // Manage references here
  "references": [
    {
      "path": "../chat"
    },
    {
      "path": "../skin"
    },
    {
      "path": "../client"
    },
    {
      "path": "../webchat"
    }
  ],

  // Defaults (don't change this)
  "extends": "../../tsconfig.packages.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "tsBuildInfoFile": "dist/.tsbuildinfo",

    // Specific settings for frontend
    "target": "es6",
    "lib": ["dom", "es2021"],
    "jsx": "react"
  },
  "exclude": ["dist", "node_modules"]
}
