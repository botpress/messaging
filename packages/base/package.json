{
  "name": "@botpress/messaging-base",
  "version": "0.0.2",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "source": "src/index.ts",
  "license": "AGPL-3.0",
  "scripts": {
    "build": "yarn --silent && yarn run -T tsc --build",
    "watch": "yarn --silent && yarn run -T tsc --build --watch",
    "prepublishOnly": "yarn --silent && yarn run -T tsc --build"
  },
  "files": [
    "dist"
  ]
}
