{
  // Manage references here
  "references": [{ "path": "../logger" }, { "path": "../../runtime/sdk" }, { "path": "../common" }],

  // Defaults (don't change this)
  "extends": "../../../tsconfig.packages.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "tsBuildInfoFile": "dist/.tsbuildinfo"
  },
  "include": ["src"],
  "exclude": ["dist"]
}
