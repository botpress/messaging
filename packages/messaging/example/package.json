{
  "name": "@botpress/messaging-channels-example",
  "version": "1.2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "source": "src/index.ts",
  "license": "AGPL-3.0",
  "scripts": {
    "build": "yarn && yarn run -T tsc --build",
    "watch": "yarn && yarn run -T tsc --build --watch",
    "start": "node ./dist/index.js",
    "dev": "yarn && yarn run -T ts-node-dev --debounce 500 --transpile-only src/index.ts"
  },
  "devDependencies": {
    "@types/cli-color": "^2.0.2",
    "@types/express": "^4.17.13"
  },
  "dependencies": {
    "@botpress/messaging-channels": "1.2.0",
    "cli-color": "^2.0.2",
    "express": "^4.18.1",
    "joi": "^17.6.0"
  }
}
