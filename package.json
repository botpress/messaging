{
  "name": "bp-messaging",
  "version": "1.0.0",
  "main": "index.ts",
  "license": "MIT",
  "scripts": {
    "build": "yarn --silent && tsc --build",
    "watch": "yarn --silent && tsc --build --watch",
    "start": "node dist/index.js",
    "dev": "yarn --silent && ts-node-dev src/index.ts",
    "clean": "rimraf dist",
    "reset": "yarn clean && rimraf node_modules"
  },
  "devDependencies": {
    "@types/node": "^15.0.0",
    "cross-env": "^7.0.3",
    "rimraf": "^3.0.2",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4"
  },
  "dependencies": {
    "@types/express": "^4.17.11",
    "express": "^4.17.1"
  }
}
